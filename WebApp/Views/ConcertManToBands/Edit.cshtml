@model WebApp.ConcertManToBand

@{
    ViewData["Title"] = "Изменить";
}

<h1>Изменить</h1>

<h4>Исполнители в группах</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ConcertManToBandId" />
            <div class="form-group">
                <label asp-for="ConcertManId" class="control-label"></label>
                <select asp-for="ConcertManId" class="form-control" asp-items="ViewBag.ConcertManId"></select>
                <span asp-validation-for="ConcertManId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="BandId" class="control-label"></label>
                <select asp-for="BandId" class="form-control" asp-items="ViewBag.BandId"></select>
                <span asp-validation-for="BandId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Сохранить" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Назад</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
